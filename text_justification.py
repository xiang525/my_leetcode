class Solution:
    # @param {string[]} words
    # @param {integer} maxWidth
    # @return {string[]}
    def fullJustify(self, words, maxWidth):

    	n = len(words)
    	count = 0
    	for i in range(n):
    		m = len(words[i])
    		if count <= maxWidth:
    			count += m
    		else:
    			
    			for j in range(i-1):
    				value += word[j]

    		
    		
    		